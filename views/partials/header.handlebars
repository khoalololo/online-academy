<nav class="bg-white shadow-lg mx-auto sticky top-0 z-10 rounded-b-lg">
    <div class="flex h-16 font-semibold items-center justify-between px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto">
        <div class="flex items-center space-x-6">
            <a class="flex items-center text-xl font-bold text-indigo-700 hover:text-indigo-900 transition-colors" href="/">Online Academy</a>
            
            <div id="navbarSupportedContent" class="hidden lg:flex items-center space-x-2">
                <a href="/" class="px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md transition-colors font-medium">Home</a>
                <a href="/products" class="px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md transition-colors font-medium">All Courses</a>
                
                <div class="relative inline-block text-left group/nav">
                    <button class="flex items-center px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md transition-all duration-150 font-medium focus:outline-none group-hover/nav:bg-gray-100">
                        Categories
                        <svg class="h-4 w-4 ml-1 transform transition-all duration-150 group-hover/nav:rotate-180" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                        </svg>
                    </button>
                    
                    <div class="absolute left-0 top-full pt-2 w-72">
                        <ul class="bg-white rounded-xl shadow-xl py-1 border border-gray-100 opacity-0 invisible transform -translate-y-2 group-hover/nav:translate-y-0 group-hover/nav:opacity-100 group-hover/nav:visible transition-all duration-150 ease-out z-50">
                            {{#each global_categories}}
                                {{#if (eq this.parent_id null)}}
                                    {{!-- Level 1 Category --}}
                                    <li>
                                        <a href="/products/list?catid={{this.id}}" 
                                            class="block px-4 py-2 text-base font-extrabold text-indigo-700 transition-colors
                                                {{#if (eq this.id ../current_category.id)}} bg-indigo-50 text-indigo-900 border-l-4 border-indigo-500{{else}} hover:bg-gray-50{{/if}}">
                                            {{this.name}}
                                        </a>
                                    </li>
                                    
                                    {{!-- Level 2 Subcategories --}}
                                    {{#each ../global_categories}}
                                        {{#if (eq this.parent_id ../this.id)}}
                                            <li>
                                                <a href="/products/list?catid={{this.id}}" 
                                                    class="block px-6 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors
                                                        {{#if (eq this.id ../../current_category.id)}} bg-gray-100 text-indigo-600 font-semibold border-l-4 border-indigo-500{{/if}}">
                                                    &bull; {{this.name}}
                                                </a>
                                            </li>
                                        {{/if}}
                                    {{/each}}
                                    
                                    {{#unless @last}}
                                        <li><hr class="my-1 border-gray-100"></li>
                                    {{/unless}}
                                {{/if}}
                            {{/each}}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="flex items-center space-x-3">
            {{#unless isAuthenticated}}
                <a href="/account/signin" class="px-4 py-2 text-sm font-semibold rounded-full text-indigo-600 border border-indigo-600 hover:bg-indigo-50 transition-colors hidden md:flex items-center">
                    Sign In
                </a>
                <a href="/account/signup" class="px-4 py-2 text-sm font-semibold rounded-full text-white bg-indigo-600 hover:bg-indigo-700 transition-colors active:bg-indigo-800 shadow-md flex items-center">
                    Sign Up
                </a>
            {{else}}
                <div class="group relative">
                    <button class="flex items-center px-4 py-2 text-sm font-semibold rounded-full text-white bg-indigo-600 hover:bg-indigo-700 transition-colors shadow-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                        Welcome, {{authUser.name}}
                        <svg class="h-4 w-4 ml-1 transform transition-all duration-150 group-hover:rotate-180" viewBox="0 0 24 24" fill="currentColor">
                            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                        </svg>
                    </button>
                    <div class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-xl py-1 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 transform scale-95 group-hover:scale-100 z-20">
                        <span class="block px-4 py-2 text-xs text-gray-500 border-b border-gray-100">Signed in as: {{authUser.username}}</span>
                        <a href="/account/profile" class="block px-4 py-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition-colors">Profile</a>
                        <a href="/account/logout" class="block px-4 py-2 text-red-600 hover:bg-red-50 transition-colors border-t border-gray-100 mt-1">Log Out</a>
                    </div>
                </div>
            {{/unless}}
            
            <div class="lg:hidden">
                <button id="mobile-menu-button" class="p-2 rounded-lg text-gray-800 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-indigo-500">
                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</nav>