import express from 'express';
const router = express.Router();

// Currently empty, but ready for future routes
// e.g., router.get('/', async (req, res) => { /* render category list */ });

export default router;